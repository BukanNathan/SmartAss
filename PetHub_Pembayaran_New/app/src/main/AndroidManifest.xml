<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pethub_pembayaran_new">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PetHub_Pembayaran_New">
        <activity
            android:name=".PaymentHistory"
            android:theme="@style/Theme.PetHub_Pembayaran_New.NoActionBar">

        </activity>
        <activity
            android:name=".PaymentMethod"
            android:label="Method">

        </activity>
        <activity
            android:name=".Basket"
            android:label="Basket">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>